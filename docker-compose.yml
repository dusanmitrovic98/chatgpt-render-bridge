version: '3.8'
services:
  chatgpt-bridge:
    build: .
    container_name: gpt_bridge
    network_mode: host  # <--- CRITICAL: Uses your PC's IP to bypass Cloudflare
    volumes:
      - gpt_profile:/data/profile 
    restart: unless-stopped

volumes:
  gpt_profile: